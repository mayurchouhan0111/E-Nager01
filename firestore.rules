rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    // Allow public read/write for all collections (development mode)
    match /{document=**} {
      allow read, write: if true;
    }
  }
}
